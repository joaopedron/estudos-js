/**
  LOAD FUNCTION:
  - Para carregar uma função automaticamente escreva a função em questão dentro de parenteses `()` e
  - inicializa ela com o uso de parentes e ponto-e-virgula ;
**/

(function function_name (argument) {
  // body...
})/* initilize function => */ ();
