// Number converte o numero passado na variavel string para o literal number
// não sendo possivel fazer a conversão ele retorn NaN (not a number)


var string = '123124';

console.log(Number(string));